---
import { Image } from 'astro:assets'
import Me from '~/images/me.png'
---

<!-- TODO: Spin on hover (requires mask) -->
<Image src={Me} alt="Palm's avatar" class="avatar" fetchpriority="high" loading="eager" />

<style>
    .avatar {
        animation: scale-in var(--dur-long) var(--ease);
        width: 256px;
        height: 256px;
    }

    @keyframes scale-in {
        from {
            transform: scale(0);
            opacity: 0;
        }
    }
</style>